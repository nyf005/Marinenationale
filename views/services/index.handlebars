<div class="fixed-action-btn">
  <a href="/services/add" class="btn-floating btn-large red">
  <i class="fa fa-plus"></i>
  </a>
</div>
<div class="container">
  <h4>Les Services</h4>
  <hr>
  {{#if services}}
  <table class="responsive-table centered highlight">
    <thead>
      <tr>
        <th>Abréviation</th>
        <th>Intitulé du service</th>
        <th class="nosort">Actions</th>
      </tr>
    </thead>
    <tbody>
      {{#each services}}
      <tr>
        <td>{{abreviation}}</td>
        <td>{{nom}}</td>
        <td>
          <a class="btn waves-effect" href="/services/edit/{{id}}">
            <i class="fa fa-pencil"></i></a>
          <form class="waves-effect" action="/services/delete/{{id}}?_method=DELETE" method="POST" id="delete-form" onclick="return ConfirmDelete();">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="btn waves-effect red"><i class="fa fa-remove"></i></button>
          </form>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  {{else}}
  <p>Aucun service n'a été ajouté pour l'instant.</p>
  {{/if}}
</div>