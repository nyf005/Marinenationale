<div class="fixed-action-btn">
  <a href="/services/add" class="btn-floating btn-large red">
  <i class="fa fa-plus"></i>
  </a>
</div>
<div class="container">
  <h4>Les Services</h4>
  <hr>
  {{#if services}}
  <table class="responsive-table centered highlight">
    <thead>
      <tr>
        <th>Abréviation</th>
        <th>Intitulé du service</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each services}}
      <tr>
        <td>{{abreviation}}</td>
        <td>{{nom}}</td>
        <td>
          <a class="btn left-align" href="/services/edit/{{id}}">
            <i class="fa fa-pencil"></i> Modifier
          </a>
        </td>
        <td>
          <form action="/services/delete/{{id}}?_method=DELETE" method="POST" id="delete-form" onclick="return ConfirmDelete();">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="btn red"><i class="fa fa-remove"></i> Supprimer</button>
          </form>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  {{else}}
  <p>Aucun service n'a été ajouté pour l'instant.</p>
  {{/if}}
</div>